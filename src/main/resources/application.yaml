spring:
  application:
    name: telemetry-service
  datasource:
    url: jdbc:postgresql://localhost:5433/telemetry_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
